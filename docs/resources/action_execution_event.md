---
page_title: "zitadel_action_execution_event Resource - terraform-provider-zitadel"
subcategory: ""
description: |-
  Resource representing an action execution triggered by an event.
---

# zitadel_action_execution_event (Resource)

Resource representing an action execution triggered by an event.

## Example Usage

```terraform
resource "zitadel_action_target" "default" {
	name               = "event-webhook"
	endpoint           = "https://example.com/webhooks/events"
	target_type        = "REST_ASYNC"
	timeout            = "10s"
	interrupt_on_error = false
}

resource "zitadel_action_execution_event" "default" {
	event = "user.human.added"
	target_ids = [zitadel_action_target.default.id]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `all` (Boolean) Trigger on all events.
- `event` (String) The specific event to trigger on, e.g., `user.human.added`
- `group` (String) The event group to trigger on, e.g., `user.human`
- `target_ids` (List of String) The list of target IDs to call.

### Read-Only

- `id` (String) The ID of this resource.

## Import

```bash
# event:name for specific event, group:name for event group, all for all events
terraform import zitadel_action_execution_event.imported 'event:user.human.added'
terraform import zitadel_action_execution_event.imported 'group:user.human'
terraform import zitadel_action_execution_event.imported 'all'
```
