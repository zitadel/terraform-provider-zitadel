---
page_title: "zitadel_action_execution_response Resource - terraform-provider-zitadel"
subcategory: ""
description: |-
  Resource representing an action execution triggered by a response.
---

# zitadel_action_execution_response (Resource)

Resource representing an action execution triggered by a response.

## Example Usage

```terraform
resource "zitadel_action_target" "default" {
	name               = "response-enricher"
	endpoint           = "https://example.com/api/enrich"
	target_type        = "REST_CALL"
	timeout            = "10s"
	interrupt_on_error = false
}

resource "zitadel_action_execution_response" "default" {
	method = "/zitadel.user.v2.UserService/GetUser"
	target_ids = [zitadel_action_target.default.id]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `all` (Boolean) Trigger on all responses.
- `method` (String) The gRPC method to trigger on, e.g., `/zitadel.session.v2.SessionService/ListSessions`
- `service` (String) The gRPC service to trigger on, e.g., `zitadel.session.v2.SessionService`
- `target_ids` (List of String) The list of target IDs to call.

### Read-Only

- `id` (String) The ID of this resource.

## Import

```bash
# method:/path for specific method, service:name for service, all for all responses
terraform import zitadel_action_execution_response.imported 'method:/zitadel.user.v2.UserService/GetUser'
terraform import zitadel_action_execution_response.imported 'service:zitadel.user.v2.UserService'
terraform import zitadel_action_execution_response.imported 'all'
```
