services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      interval: 10s
      timeout: 60s
      retries: 5
      start_period: 10s

  proxy:
    image: serjs/go-socks5-proxy:latest
    ports:
      - "1080:1080"

  zitadel:
    user: '${ZITADEL_DEV_UID}'
    image: '${ZITADEL_IMAGE:-ghcr.io/zitadel/zitadel:v4.7.0}'
    command: 'start-from-init --masterkey "MasterkeyNeedsToHave32Characters" --tlsMode disabled --config /zitadel.yaml --steps /zitadel.yaml'
    ports:
      - "8080:8080"
    volumes:
      - ./keys:/keys
      - ./zitadel.yaml:/zitadel.yaml
    healthcheck:
      test: ['CMD', '/app/zitadel', 'ready', "--config", "/zitadel.yaml"]
      interval: '2s'
      timeout: '30s'
      retries: 60
    depends_on:
      db:
        condition: 'service_healthy'
      proxy:
        condition: 'service_started'
    networks:
      default:
        aliases:
          - org-level-tests.default.127.0.0.1.sslip.io
          - instance-level-tests.default.127.0.0.1.sslip.io

  setup:
    user: '${ZITADEL_DEV_UID}'
    container_name: setup
    build: .
    environment:
      KEYS_DIRECTORY: /keys
      SERVICE: http://zitadel:8080
    volumes:
      - "./keys:/keys"
    depends_on:
      zitadel:
        condition: 'service_healthy'
